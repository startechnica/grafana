// k8s_events
//
// Kubernetes events as log lines. loki.source.kubernetes_events watches the
// API server for the events of every namespace and ships each one as a line,
// in JSON (the default) or logfmt. The pipeline parses either format, turns
// the event's type, reason, object kind, reporting component and node into
// labels, and drops Normal events unless drop_normal_events = false.
//
// Safe on every pod of a clustered DaemonSet: with Alloy clustering the peers
// pick one of them to watch the events, so each event is shipped once.
// Without clustering, every Alloy that runs this ships every event.
//
// Labels:
//   job        integrations/kubernetes/eventhandler, or job_label
//   instance   the ID of the source component
//   namespace  the namespace of the object the event is about (none for
//              cluster-scoped objects such as nodes)
//   type       Normal or Warning
//   reason     e.g. BackOff, FailedScheduling, Unhealthy
//   kind       the object's kind, e.g. Pod, Node
//   source     the component that reported it, e.g. kubelet, default-scheduler
//   node       the node it was reported from, when there is one
//   cluster_name
// The line keeps the rest: the object's name, the message (msg), the count.
declare "kubernetes" {
	argument "cluster_name" { }

	argument "forward_to" {
		comment = "Must be a list(LogsReceiver) where collected logs should be forwarded to"
	}

	argument "job_label" {
		comment  = "The job label of the event streams (default: integrations/kubernetes/eventhandler)"
		optional = true
	}

	argument "log_format" {
		comment  = "Line format of each event, \"json\" or \"logfmt\" (default: \"json\")"
		optional = true
	}

	argument "tenant_id" { }

	argument "drop_normal_events" {
		comment  = "Drop events of type Normal, keeping Warning ones only (default: true)"
		default  = true
		optional = true
	}

	loki.source.kubernetes_events "cluster_events" {
		job_name   = coalesce(argument.job_label.value, "integrations/kubernetes/eventhandler")
		log_format = coalesce(argument.log_format.value, "json")
		forward_to = [
			loki.process.cluster_events.receiver,
		]

		// Watching all namespaces is a single clustering key, so exactly one
		// peer owns it.
		clustering {
			enabled = true
		}
	}

	loki.process "cluster_events" {
		forward_to = argument.forward_to.value

		// Parse by the line's own format, since log_format can pick either.
		// The field names are the ones loki.source.kubernetes_events writes,
		// the same in both formats.
		stage.match {
			selector      = `{job=~".+"} |~ "^[{]"`
			pipeline_name = "json"

			stage.json {
				expressions = {
					type   = "type",
					reason = "reason",
					kind   = "kind",
					source = "sourcecomponent",
					node   = "sourcehost",
				}
			}
		}

		stage.match {
			selector      = `{job=~".+"} !~ "^[{]"`
			pipeline_name = "logfmt"

			stage.logfmt {
				mapping = {
					type   = "",
					reason = "",
					kind   = "",
					source = "sourcecomponent",
					node   = "sourcehost",
				}
			}
		}

		stage.labels {
			values = {
				type   = "", // Normal or Warning
				reason = "", // e.g. BackOff, Pulled, Scheduled
				kind   = "", // e.g. Pod, Deployment
				source = "", // e.g. kubelet, default-scheduler
				node   = "",
			}
		}

		// After stage.labels: a selector matches labels, not extracted fields.
		// The "false" branch selects a label no entry carries, a valid selector
		// that matches nothing, since Alloy has no conditional blocks.
		stage.match {
			selector = {
				"true"  = `{type="Normal"}`,
				"false" = `{k8s_events_never_matches="1"}`,
			}[string.format("%t", argument.drop_normal_events.value)]

			action              = "drop"
			drop_counter_reason = "normal_events"
		}

		stage.static_labels {
			values = {
				cluster_name = argument.cluster_name.value,
			}
		}

		stage.tenant {
			value = argument.tenant_id.value
		}
	}
}
